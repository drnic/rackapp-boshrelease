set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

export HOME=/var/vcap

PATH=/var/vcap/packages/python27/bin:$PATH

cp -a RedisLive/* ${BOSH_INSTALL_TARGET}

pip install tornado         # TODO tornado-2.3.tar.gz
pip install redis           # TODO redis-2.6.0.tar.gz
pip install python-dateutil # TODO python-dateutil-2.1.tar.gz
